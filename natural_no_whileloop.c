#include <stdio.h> // print natural no from 20 to 40 using while loop
int main()
{
    int i = 0;

    while (i <= 40)
    {
        if (i >= 20)
        {
            printf("the value of i is : %d\n", i);
           
        }
        i++;
    }
}