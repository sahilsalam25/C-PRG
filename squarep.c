#include<stdio.h>
int main()
{
    int side;
    int area;
    printf("enter the value of side\n");
    scanf("%d",&side);
    area=(side*side);
    printf("value of area is %d",area);
    return 0;

}